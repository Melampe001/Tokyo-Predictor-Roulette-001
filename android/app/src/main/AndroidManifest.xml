<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Tokyo Roulette by TokyoApps® -->
    <!-- Simulación inteligente para entretenimiento -->
    
    <!-- Permisos necesarios para la aplicación -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Feature declarations for Play Store -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:allowBackup="true"
        android:supportsRtl="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Especifica que esta Activity es el punto de entrada de la app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Metadatos de Flutter (no eliminar) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- App metadata for branding -->
        <meta-data
            android:name="com.tokyoapps.brand"
            android:value="TokyoApps®" />
        <meta-data
            android:name="com.tokyoapps.slogan"
            android:value="Simulación inteligente para entretenimiento" />
    </application>
</manifest>